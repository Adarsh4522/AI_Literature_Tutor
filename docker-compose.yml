services:
  litwise:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: litwise
    ports:
      - "80:5000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend/cache:/app/backend/cache
    restart: unless-stopped